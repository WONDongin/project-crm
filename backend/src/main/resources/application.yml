spring:
  application:
    name: crm-backend

  datasource:
    url: jdbc:mysql://localhost:3306/crm?serverTimezone=Asia/Seoul
    username: root
    password: 1228
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
